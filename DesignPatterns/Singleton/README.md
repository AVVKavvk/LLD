# Singleton

---

- Definition: Ensures a class has only one instance and provides a global point of access to it.
- Use Case: Database connections, logging classes, configuration settings.

- What it solves: Ensures a class has only one instance and provides a global point of access to it.

- What it prevents: Prevents the wasteful creation of multiple resource-heavy objects.
